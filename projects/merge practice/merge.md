# Merge Workflow

This exercise merges a feature branch into the main branch.

Ex workflow:

git checkout -b feature-update
git add .
git commit -m "Added feature"
git checkout main
git merge feature-update
